# CLSMC V23.1 Deploy

1. ZIP içeriğini GitHub repository ana dizinine yükleyin.
2. `requirements.txt`, `render.yaml` ve Render Environment ayarlarını değiştirmeyin.
3. Commit mesajı: `CLSMC V23.1 - Add admin account management`
4. Render deploy tamamlandıktan sonra `/admin/accounts` adresini kontrol edin.
